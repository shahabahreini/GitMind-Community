# Generate your first commit message

1. Stage some changes in the **Source Control** panel, or enable **Capture All Changes** to include unstaged and untracked files.
2. Click the **GitMind** icon in the Source Control toolbar (or run **Generate GitMind Commit Message** from the Command Palette).
3. Review included, summarized, and excluded changes; add optional intent; then inspect and approve the provider/model/destination/token preview.
4. Edit or select a valid draft and explicitly insert it into the selected repository's Source Control message box.

You can pick a **commit style** (Conventional, Semantic, Descriptive, Gitmoji, and more) in Settings, and add custom context to any generation.

GitMind Pro can also retry eligible temporary failures once and switch to a configured fallback model once when the selected model reaches a model-specific limit.
