# Cohere
> Verified against GitMind `5.0.6` on June 7, 2026.

Create a key in the [Cohere dashboard](https://dashboard.cohere.com/api-keys). The default is `command-a`; model discovery reflects the provider catalog. GitMind sends the selected diff and prompt to Cohere. Validate model access and account limits after authentication errors.
