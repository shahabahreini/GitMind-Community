# Custom API
> Verified against GitMind `6.1.1` on July 17, 2026

Custom API is a Pro provider for private gateways and compatible endpoints. Configure the base URL, endpoint, model, authentication, request format, and response format, then test before generation. Data goes to the endpoint you configure. See [Custom API Guide](../Custom-API-Guide) for all fields and troubleshooting.
