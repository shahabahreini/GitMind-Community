# Groq
> Verified against GitMind `6.1.1` on September 3, 2026

Create a key in the [Groq console](https://console.groq.com/keys). The default is `meta-llama/llama-4-scout-17b-16e-instruct`; **Load Groq Models** refreshes the searchable catalog. GitMind sends the selected diff and prompt to Groq. Confirm model availability and account rate limits after errors.
